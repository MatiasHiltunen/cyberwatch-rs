apiVersion: apps/v1
kind: Deployment
metadata:
  name: cyberwatch
spec:
  template:
    spec:
      securityContext:
        runAsUser: 10001
        runAsGroup: 0
        fsGroup: 10001
        fsGroupChangePolicy: OnRootMismatch
